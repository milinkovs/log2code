spring:
  application:
    name: code-service
  config:
    import: optional:configserver:http://localhost:8888/

---
spring:
  config:
    activate:
      on-profile: docker
    import: configserver:http://config-server:8888
