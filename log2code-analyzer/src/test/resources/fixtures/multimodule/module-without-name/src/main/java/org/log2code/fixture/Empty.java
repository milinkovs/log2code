package org.log2code.fixture;

// No application.{yml,yaml,properties} in this module, mirroring PetClinic's config-server
// module, which has no spring.application.name anywhere (see progress.md, T07).
class Empty {
}
