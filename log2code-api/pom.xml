<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.log2code</groupId>
    <artifactId>log2code-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>log2code-api</artifactId>
  <name>log2code-api</name>

  <properties>
    <spring-boot.version>4.0.8</spring-boot.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Boot BOM only here: the parent must not pull Spring versions into the CLI modules -->
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.log2code</groupId>
      <artifactId>log2code-core</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- TestRestTemplate: Boot 4 split it out of spring-boot-test into its own module -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-resttestclient</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- RestTemplateBuilder: needed by TestRestTemplateTestAutoConfiguration (@AutoConfigureTestRestTemplate) above -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-restclient</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- @WebMvcTest: likewise split out of spring-boot-starter-test in Boot 4 -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Pin explicitly: JUnit 6 here (Boot BOM) vs. 5 elsewhere (parent BOM, 0.6) means the
         surefire/failsafe JUnit Platform provider's own default launcher version can mismatch
         jupiter-engine 6.0.3, breaking test discovery. Declaring it lets the provider pick up the
         already-correct 6.0.3 (effective-pom-verified) from the project classpath instead. -->
    <dependency>
      <groupId>org.junit.platform</groupId>
      <artifactId>junit-platform-launcher</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.opensearch</groupId>
      <artifactId>opensearch-testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- classifier (0.13): without it, repackage REPLACES the plain jar (flat class layout) as
           this module's primary artifact with the Boot fat jar (classes nested under BOOT-INF/classes/).
           failsafe's integration-test phase runs after package/repackage, and its classpath then picks
           up that nested-layout artifact instead of target/classes, so top-level classes stop resolving
           (NoClassDefFoundError for org.log2code.api.dto.* during ApiIT discovery). The classifier keeps
           the flat jar as the primary artifact; the executable jar is still built, as *-exec.jar. -->
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <version>${spring-boot.version}</version>
        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
            <configuration>
              <classifier>exec</classifier>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
